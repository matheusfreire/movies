<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.msf.movies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.msf.movies.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.msf.movies.activity.MovieListActivity"
            android:label="@string/title_movie_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.msf.movies.activity.MovieDetailActivity"
            android:parentActivityName="com.msf.movies.activity.MovieListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.msf.movies.activity.MovieListActivity" />
        </activity>
        <activity
            android:name="com.msf.movies.activity.MainActivity"
            android:label="@string/title_activity_main"/>
    </application>

</manifest>